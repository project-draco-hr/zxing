{
  if (Build.MODEL.contains("Behold II") && CameraManager.SDK_INT == Build.VERSION_CODES.CUPCAKE) {
    parameters.set("flash-value",1);
  }
 else {
    parameters.set("flash-value",2);
  }
  parameters.set("flash-mode","off");
}
