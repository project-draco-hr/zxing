{
  String result=null;
  if (camera == null) {
    camera=Camera.open();
    camera.setPreviewDisplay(holder);
    if (!initialized) {
      initialized=true;
      getScreenResolution();
    }
    if (getParameters) {
      result=collectCameraParameters();
    }
    setCameraParameters();
  }
  return result;
}
