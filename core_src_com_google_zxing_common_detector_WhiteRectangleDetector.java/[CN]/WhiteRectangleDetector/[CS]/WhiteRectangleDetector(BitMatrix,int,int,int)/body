{
  this.image=image;
  height=image.getHeight();
  width=image.getWidth();
  int halfsize=INIT_SIZE >> 1;
  leftInit=x - halfsize;
  rightInit=x + halfsize;
  upInit=y - halfsize;
  downInit=y + halfsize;
}
