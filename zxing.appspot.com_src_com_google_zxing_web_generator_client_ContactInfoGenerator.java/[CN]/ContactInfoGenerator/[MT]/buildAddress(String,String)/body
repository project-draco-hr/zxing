{
  if (address.length() > 0) {
    if (address2.length() > 0) {
      return address + ' ' + address2;
    }
    return address;
  }
  if (address2.length() > 0) {
    return address2;
  }
  return "";
}
