{
  if (email == null) {
    return false;
  }
  boolean atFound=false;
  for (int i=0; i < email.length(); i++) {
    char c=email.charAt(i);
    if (c == '@') {
      atFound=true;
    }
 else     if (c == '.') {
      if (!atFound) {
        return false;
      }
    }
 else     if (c == ' ' || c == '\n') {
      return false;
    }
  }
  return true;
}
