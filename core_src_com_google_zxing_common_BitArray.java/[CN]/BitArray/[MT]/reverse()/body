{
  int[] newBits=makeArray(size);
  int max=newBits.length;
  for (int i=0; i < max; i++) {
    newBits[i]=0;
  }
  int size=this.size;
  for (int i=0; i < size; i++) {
    if (get(size - i - 1)) {
      newBits[i >> 5]|=1 << (i & 0x1F);
    }
  }
  bits=newBits;
}
