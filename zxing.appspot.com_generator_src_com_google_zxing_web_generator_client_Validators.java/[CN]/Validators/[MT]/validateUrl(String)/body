{
  if (!((url.startsWith("http://") && url.length() > 7) || (url.startsWith("https://") && url.length() > 8))) {
    throw new GeneratorException("URL: http:// or https://," + "plus at least 1 character.");
  }
}
