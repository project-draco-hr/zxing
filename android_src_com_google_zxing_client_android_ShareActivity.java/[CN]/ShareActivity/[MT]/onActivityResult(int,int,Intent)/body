{
  if (resultCode == RESULT_OK) {
switch (requestCode) {
case PICK_BOOKMARK:
      Log.v("BOOKMARK",intent.toString());
    break;
case PICK_CONTACT:
  showContactAsBarcode(intent.getData());
break;
}
}
}
