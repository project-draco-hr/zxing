{
  String formats=intent.getStringExtra(Intents.Scan.SCAN_FORMATS);
  if (formats != null) {
    return parseDecodeFormats(Arrays.asList(COMMA_PATTERN.split(formats)),intent.getStringExtra(Intents.Scan.MODE));
  }
  return null;
}
