def isD(source):
    if (not source):
        return 0
    for s in source:
        if s.sources:
            ext = os.path.splitext(str(s.sources[0]))[1]
            if (ext == '.d'):
                return 1
    return 0
