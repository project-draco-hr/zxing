{
  String text=result.getDisplayResult();
switch (index) {
case 0:
    webSearch(text);
  break;
case 1:
shareByEmail(text);
break;
case 2:
shareBySMS(text);
break;
case 3:
String url=customProductSearch.replace("%s",text);
openURL(url);
break;
}
}
