def exists(env):
    (path, _cc, _shcc, version) = aixcc.get_xlc(env)
    if (path and _cc):
        xlc = os.path.join(path, _cc)
        if os.path.exists(xlc):
            return xlc
    return None
