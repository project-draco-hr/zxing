{
  ResultPoint[] points=rawResult.getResultPoints();
  if (points != null && points.length > 0) {
    surfaceView.drawResultPoints(points);
  }
  Context context=getApplication();
  ParsedReaderResult readerResult=parseReaderResult(rawResult);
  ResultHandler handler=new ResultHandler(this,readerResult);
  if (handler.getIntent() != null) {
    showAlert(context.getString(getDialogTitleID(readerResult.getType())),0,readerResult.getDisplayResult(),context.getString(R.string.button_yes),handler,context.getString(R.string.button_no),null,true,null);
  }
 else {
    showAlert(context.getString(R.string.title_barcode_detected),0,readerResult.getDisplayResult(),context.getString(R.string.button_ok),true);
  }
}
