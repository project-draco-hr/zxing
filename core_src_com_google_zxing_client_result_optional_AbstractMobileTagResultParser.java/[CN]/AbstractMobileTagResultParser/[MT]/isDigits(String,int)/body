{
  if (s == null) {
    return true;
  }
  if (s.length() != expectedLength) {
    return false;
  }
  for (int i=0; i < expectedLength; i++) {
    if (!Character.isDigit(s.charAt(i))) {
      return false;
    }
  }
  return true;
}
