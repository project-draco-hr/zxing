def readline(self):
    result = []
    while True:
        line = self.fileobj.readline()
        if (not line):
            break
        if (line[(-2):] == '\\\n'):
            result.append(line[:(-2)])
        else:
            result.append(line)
            break
    return ''.join(result)
