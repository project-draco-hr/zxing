{
  if (column == null || column.length < height) {
    column=new int[height];
  }
  image.getRGB(column,0,1,x,0,1,height);
  for (int y=0; y < height; y++) {
    int pixel=column[y];
    column[y]=(((pixel & 0x00FF0000) >> 16) + ((pixel & 0x0000FF00) >> 7) + (pixel & 0x000000FF)) >> 2;
  }
  return column;
}
